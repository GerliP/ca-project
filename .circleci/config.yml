version: 2.0
jobs:
  test:
    docker:
      - image: python:3
    steps: 
      - checkout
      - run: 
          command: |
            pip install --trusted-host pypi.python.org -r requirements.txt 
            python tests.py
workflows: 
  version: 2
  test: 
    jobs: 
      - test
